{
  "name": "shared",
  "version": "1.0.0",
  "description": "Shared business models, terminology presets, and API interfaces across OrderFlow Web & Mobile",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^4.1.11"
  },
  "private": true
}
