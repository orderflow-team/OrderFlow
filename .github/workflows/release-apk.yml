name: Release APK (direct distribution)

# Builds a release-signed APK for the `direct` flavor — the self-hosted,
# non-Play-Store distribution channel (landing page's "Download APK" button)
# that self-updates via REQUEST_INSTALL_PACKAGES. Publishes it to the
# backend, and the /api/app-apk-releases/latest endpoint starts serving it to
# the in-app updater. Also updates the public download-site link automatically.
#
# For the Play Store submission (a signed AAB, no self-update permission),
# use release-playstore.yml instead — see that workflow and
# apps/web/android/app/build.gradle's productFlavors comment for why these
# are two separate builds, not one.
#
# Required GitHub secrets:
#   NEXT_PUBLIC_API_URL  — production API base URL (e.g. https://orderflow-1.onrender.com)
#   ADMIN_TOKEN          — JWT for a super_admin or admin user on that API
#   KEYSTORE_BASE64      — base64-encoded release keystore file
#                          (base64 -w 0 your-release.jks)
#   KEYSTORE_PASSWORD    — storePassword from keystore.properties
#   KEY_ALIAS            — keyAlias from keystore.properties
#   KEY_PASSWORD         — keyPassword from keystore.properties
#
# Before running: bump versionCode and versionName in
#   apps/web/android/app/build.gradle

on:
  workflow_dispatch:
    inputs:
      version_name:
        description: 'Version name — must match versionName in build.gradle (e.g. 1.16)'
        required: true
        type: string
      notes:
        description: 'Release notes shown to users in the update prompt'
        required: false
        type: string

jobs:
  release:
    name: Build & publish signed APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install dependencies
        run: npm ci

      - name: Build Capacitor web export
        working-directory: apps/web
        env:
          CAPACITOR_BUILD: '1'
          NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}
          NEXT_PUBLIC_DISTRIBUTION_CHANNEL: direct
        run: npm run build:capacitor

      - name: Sync Capacitor to Android
        working-directory: apps/web
        run: npx cap sync android

      - name: Write keystore file
        working-directory: apps/web/android
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > release.jks

      - name: Write keystore.properties
        working-directory: apps/web/android
        run: |
          cat > keystore.properties <<EOF
          storeFile=release.jks
          storePassword=${{ secrets.KEYSTORE_PASSWORD }}
          keyAlias=${{ secrets.KEY_ALIAS }}
          keyPassword=${{ secrets.KEY_PASSWORD }}
          EOF

      - name: Build release APK (direct flavor)
        working-directory: apps/web/android
        run: |
          chmod +x gradlew
          ./gradlew assembleDirectRelease --no-daemon

      - name: Publish APK
        working-directory: apps/web
        env:
          NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}
          ADMIN_TOKEN: ${{ secrets.ADMIN_TOKEN }}
        run: |
          APK_PATH="android/app/build/outputs/apk/direct/release/app-direct-release.apk"
          NOTES="${{ inputs.notes }}"
          if [ -n "$NOTES" ]; then
            node scripts/release-apk.mjs "${{ inputs.version_name }}" "$APK_PATH" "$NOTES"
          else
            node scripts/release-apk.mjs "${{ inputs.version_name }}" "$APK_PATH"
          fi

      - name: Upload APK as workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: obix-${{ inputs.version_name }}.apk
          path: apps/web/android/app/build/outputs/apk/direct/release/app-direct-release.apk
          retention-days: 30
